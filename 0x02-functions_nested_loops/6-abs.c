#include "main.h"

/**
 * _abs - Find the absolute value of a number
 *
 * @n: input feild for checking
 * Return: return the absolute value of the input number
 */
int _abs(int n)
{
	if (n < 0)
		n = n * -1;
	return (n);
}
